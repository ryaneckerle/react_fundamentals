// Props
// Short for properties
// Parameters of a component function that gets passed into the component
// import React from 'react'

const NameProp = (props) => {
  return <h1>Hello, {props.name}</h1>;
};

export default NameProp;
